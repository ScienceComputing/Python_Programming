pip install -e .
# Install the project as a package in editable mode. 
# Any changes we make to the source code within that project directory will be immediately reflected in the installed package.
# It is commonly used during development when we are actively working on the code and want to test changes without reinstalling the package after each modification.
